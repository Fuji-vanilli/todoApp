server:
  port: 1200

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TODO-DB
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true